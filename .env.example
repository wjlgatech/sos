# Copy this file to .env and fill in your values.
# Real keys live in ~/.secrets — sourced automatically in every terminal.
# This file documents which keys THIS project needs.

ANTHROPIC_API_KEY=           # Optional — Claude Haiku for AI-enhanced narrative sections
                             # Without this key the system still works via rule-based analysis
